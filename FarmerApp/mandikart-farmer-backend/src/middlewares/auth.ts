/**
 * MandiKart — Auth Middleware Re-export
 * Inherits standard JWT verification and role checks from @mandikart/shared-core.
 */

export { requireAuth, requireRole, type AuthenticatedUser } from '@mandikart/shared-core';
